name: Build and Deploy to Azure Container Registry

on:
  push:
    branches:
      - staging
      - "staging/**"
  workflow_dispatch: # ให้รันได้ด้วยมือ

env:
  IMAGE_NAME: kk-transcription
  WHISPER_IMAGE_NAME: kk-transcription-whisper
  REGISTRY: ${{ secrets.ACR_LOGIN_SERVER }}.azurecr.io
  VERSION: ${{ github.sha }}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      main-image: ${{ steps.save-main.outputs.imageid }}
      whisper-image: ${{ steps.save-whisper.outputs.imageid }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build main application
        id: build-main
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=registry

      - name: Build whisper service
        id: build-whisper
        uses: docker/build-push-action@v5
        with:
          context: ./whisper-service
          file: ./whisper-service/Dockerfile
          push: false
          tags: ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:${{ env.VERSION }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=registry

      - name: Save main image ID
        id: save-main
        run: |
          echo "imageid=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}" >> $GITHUB_OUTPUT

      - name: Save whisper image ID
        id: save-whisper
        run: |
          echo "imageid=${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:${{ env.VERSION }}" >> $GITHUB_OUTPUT

      - name: Update docker-compose.staging.yml with image tags
        run: |
          # สร้าง staging compose file ที่ใช้ ACR images
          cat > docker-compose.staging.acr.yml << EOF
          services:
            api:
              image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}
              container_name: transcription-api-staging
              ports:
                - "8001:8001"
              volumes:
                - ./uploads:/app/uploads
                - ./storage:/app/storage
                - ./temp:/app/temp
                - ./models:/app/models
              environment:
                - PYTHONPATH=/app
                - ENVIRONMENT=staging
                - STORAGE_TYPE=sqlite
                - SQLITE_DB_PATH=/app/storage/database.db
                - RABBITMQ_HOST=rabbitmq
                - RABBITMQ_PORT=5672
                - RABBITMQ_USER=admin
                - RABBITMQ_PASSWORD=admin123
              depends_on:
                - redis
                - rabbitmq
              networks:
                - app-network
              restart: unless-stopped
              healthcheck:
                test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
                interval: 30s
                timeout: 10s
                retries: 3
                start_period: 40s
              deploy:
                resources:
                  limits:
                    memory: 6G
                    cpus: '3.0'

            whisper:
              image: ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:${{ env.VERSION }}
              container_name: transcription-whisper-staging
              ports:
                - "8002:8002"
              volumes:
                - ./uploads:/app/audio
                - ./temp:/app/temp
                - ./models:/app/models
              environment:
                - WHISPER_MODEL_PATH=/app/models
                - ENVIRONMENT=staging
              networks:
                - app-network
              restart: unless-stopped
              healthcheck:
                test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
                interval: 30s
                timeout: 10s
                retries: 3
                start_period: 40s
              deploy:
                resources:
                  limits:
                    memory: 8G
                    cpus: '6.0'

            # เพิ่ม services อื่นๆ ตามต้องการ
            rabbitmq:
              image: rabbitmq:3-management-alpine
              container_name: transcription-rabbitmq-staging
              ports:
                - "5672:5672"
                - "15672:15672"
              environment:
                - RABBITMQ_DEFAULT_USER=admin
                - RABBITMQ_DEFAULT_PASS=admin123
              volumes:
                - rabbitmq_data_staging:/var/lib/rabbitmq
              networks:
                - app-network
              restart: unless-stopped

            redis:
              image: redis:7-alpine
              container_name: transcription-redis-staging
              ports:
                - "6379:6379"
              volumes:
                - redis_data_staging:/data
              networks:
                - app-network
              restart: unless-stopped
              command: redis-server --appendonly yes --maxmemory 2gb --maxmemory-policy allkeys-lru

          volumes:
            redis_data_staging:
            rabbitmq_data_staging:

          networks:
            app-network:
              driver: bridge
          EOF

      - name: Upload staging compose file
        uses: actions/upload-artifact@v4
        with:
          name: docker-compose-staging-acr
          path: docker-compose.staging.acr.yml

      - name: Output build info
        run: |
          echo "✅ Images built successfully!"
          echo "Main App: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"
          echo "Whisper: ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:${{ env.VERSION }}"
          echo "Registry: ${{ env.REGISTRY }}"
          echo "Staging Compose File: docker-compose.staging.acr.yml"

  push:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ env.REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Push main application
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging-latest
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Push whisper service
        uses: docker/build-push-action@v5
        with:
          context: ./whisper-service
          file: ./whisper-service/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:${{ env.VERSION }}
            ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:staging-latest
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Output push info
        run: |
          echo "✅ Images pushed successfully!"
          echo "Main App Tags:"
          echo "  - ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"
          echo "  - ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging-latest"
          echo "Whisper Tags:"
          echo "  - ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:${{ env.VERSION }}"
          echo "  - ${{ env.REGISTRY }}/${{ env.WHISPER_IMAGE_NAME }}:staging-latest"
          echo "Registry: ${{ env.REGISTRY }}"
